# AI 嘴替卡 V0.8 交互规范

## 1. 顶部上下文

### 人物

- 头像左右箭头切换人物；
- 点击头像打开人物详情；
- 首页仅显示名称、角色和当前情绪。
- 详情使用“画像与类型、事实与依据、记录与补充”三个页签。
- 人物类型在详情首屏突出展示，作为推荐判断的重要输入。

### 事项

- 显示事项名称、阶段和主要矛盾；
- 点击打开事项详情。
- 详情使用“项目与判断、进展与风险、记录与补充”三个页签。

### 历史与卡包

- “回合”包含所有完成记录；
- “卡包”只包含用户星标收藏的记录。

## 2. 对方场景牌堆

左侧使用三张错叠卡背表现。

点击后可选择：

- 已有场景；
- 录入现实回复；
- 输入用户疑问；
- AI模拟下一句。

已有场景使用与牌桌相同的场景卡面，以紧凑牌墙展示；卡面包含当前人物头像，翻面不触发出牌。

新场景打出后：

1. 替换或进入当前场景位；
2. 手牌全部收回；
3. 按新场景重新发出3—5张牌；
4. 发牌动画从右侧进入下方扇形区。

## 3. 四张场面牌

场面显示：

- 对方上一轮；
- 我方上一轮；
- 对方当前轮；
- 我方当前轮。

桌面端以当前轮两张主卡为视觉中心；上一轮缩小进入右上回合托盘，但仍可翻面查看。移动端上一轮托盘回到当前轮上方，避免压缩卡片可读性。

场景牌和应对牌均支持翻面。

牌桌空间遵循：

```text
左侧对方场景牌堆 → 中央当前交锋 → 右侧卡包与回合归档
```

不设置虚构的“我方牌堆”；我方可用牌就是下方扇形手牌。

## 4. 手牌

### 扇形交互

- 3—5张；
- 悬停明显上移；
- 相邻卡牌向两侧让位；
- 滚轮移动当前焦点；
- 场景改变时重新发牌。
- AI主推荐固定在视觉中位，使用独立金色框角、宝石和低强度呼吸光效；
- 其他推荐等级只保留轻量边缘提示，避免同时争夺注意力。

### 正面

显示：

- 推荐身份；
- 路线名称；
- 最终建议话术；
- 一句AI判断。

### 背面

显示：

- 解法逻辑；
- AI完整判断；
- 不适用情况；
- 方法依据。

## 5. 出牌

点击手牌进入出牌弹层。

顺序：

1. 展示AI推荐身份和判断；
2. 非推荐牌显示风险提示；
3. 抉择牌先选择具体路线；
4. 切换表达风格；
5. 预览最终回复；
6. 确认出牌。

确认后，手牌飞入当前我方出牌位。

## 6. 收藏

我方出牌后出现：

- ☆ 收藏这一回合；
- ★ 已收藏到卡包。

收藏状态跟随回合归档。

用户可以在历史或卡包中再次切换收藏状态。

## 7. 结束回合

### 等待现实回复

- 当前回合合并归档；
- 当前场景清空；
- 提示从对方牌堆录入现实回复。

### 模拟下一句

- 当前回合合并归档；
- AI生成模拟场景牌；
- 新场景进入当前轮；
- 手牌重新发出。

## 8. 卡包浏览

卡包使用完整回合牌：

正面上半部分为场景，下半部分为应对。

首页入口使用右侧竖向卡册书脊；收藏卡正面上半部分沿用蓝色场景牌材质，下半部分沿用米色我方应对牌材质。

支持：

- 搜索；
- 按人物筛选；
- 翻面；
- 取消收藏。

P1增加分享、导出和按验证结果排序。

## 9. 本地保存与文件迁移

- 人物、事项、会话、历史和卡包发生业务变化后自动写入 `localStorage`；
- 手牌焦点、弹窗、筛选词和翻面状态不持久化；
- 用户可导出带版本号的 JSON 状态文件；
- 导入前必须校验版本、人物、事项、会话和卡包结构；
- 导入成功后重新载入页面，错误文件不得覆盖现有状态。
