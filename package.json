{
  "name": "ai-zui-ti-card",
  "version": "0.9.0",
  "private": true,
  "description": "AI 嘴替卡：场景化表达教练，替你在不同人、不同场合说出最合适的话",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build.mjs",
    "check": "npm test && npm run build"
  }
}
