{
  "name": "relationship-decision-cards",
  "version": "0.9.0",
  "private": true,
  "description": "关系决策牌组静态原型",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build.mjs",
    "check": "npm test && npm run build"
  }
}
